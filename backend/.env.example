# ===========================================
# JobFinder+ Backend Environment Variables
# ===========================================
# Copy this file to .env and fill in the values

# Database (Neon PostgreSQL recommended)
DATABASE_URL=postgresql://username:password@host:5432/database?sslmode=require

# JWT Authentication
JWT_SECRET=your-super-secret-jwt-key-minimum-32-characters
JWT_EXPIRES_IN=7d

# Google OAuth 2.0
GOOGLE_CLIENT_ID=your-google-client-id.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=your-google-client-secret
GOOGLE_CALLBACK_URL=http://localhost:5000/api/auth/google/callback

# Groq AI API (for AI Job Match & Resume features)
GROQ_API_KEY=gsk_your-groq-api-key

# Indian Jobs API (10 requests/month - run weekly only!)
# Get key from: https://indianapi.in/jobs-api
INDIAN_API_KEY=sk-live-your-key-here

# Server Configuration
PORT=5000
NODE_ENV=development

# Frontend URL (for CORS and redirects)
FRONTEND_URL=http://localhost:5173

# Job API Keys (Optional - some work without keys)
ADZUNA_APP_ID=your-adzuna-app-id
ADZUNA_APP_KEY=your-adzuna-app-key
# Arbeitnow, Remotive, TheMuse work without API keys

# UPI Payment Configuration
UPI_ID=your-upi-id@upi

# ===========================================
# IMPORTANT NOTES:
# ===========================================
# 1. Never commit .env file to version control
# 2. Keep JWT_SECRET and API keys secure
# 3. Use different values for production
# 4. Neon PostgreSQL: https://neon.tech (free tier available)
# 5. Groq AI: https://console.groq.com (free tier available)
# 6. Google OAuth: https://console.cloud.google.com
